# Современный стиль кнопок "мыльницы" для категорий

## Дата: 19 января 2026

## Изменения

### Файл: `src/pages/CatalogPage.tsx`

Обновлены три кнопки фильтрации товаров ("Все товары", "🎁 Товары", "📦 Наборы") в современном стиле pill-buttons (мыльницы).

## Новый дизайн

### Контейнер
- **Фон**: Светлый серый с прозрачностью (`bg-muted/50`)
- **Рамка**: Тонкая рамка с прозрачностью (`border border-border/40`)
- **Скругление**: Большое скругление (`rounded-2xl`)
- **Тень**: Легкая тень (`shadow-sm`)
- **Отступы**: Внутренние отступы (`p-1.5`)

### Активная кнопка
- **Фон**: Градиент от primary к primary/90 (`bg-gradient-to-r from-primary to-primary/90`)
- **Текст**: Цвет текста primary-foreground
- **Тень**: Цветная тень с эффектом свечения (`shadow-md shadow-primary/30`)
- **Hover**: Увеличенная тень и масштаб (`hover:shadow-lg hover:shadow-primary/40 hover:scale-[1.02]`)
- **Анимация**: Плавные переходы (`transition-all duration-300`)

### Неактивная кнопка
- **Фон**: Прозрачный с hover эффектом (`hover:bg-background/60`)
- **Текст**: Приглушенный цвет с hover эффектом (`text-muted-foreground hover:text-foreground`)
- **Анимация**: Плавные переходы (`transition-all duration-300`)

### Общие стили
- **Размер**: Адаптивная высота (`h-9 sm:h-10`)
- **Шрифт**: Полужирный (`font-semibold`)
- **Размер текста**: Адаптивный (`text-xs sm:text-sm`)
- **Скругление**: Большое (`rounded-xl`)
- **Ширина**: Равномерное распределение (`flex-1`)

## Визуальные эффекты

1. **Градиент**: Активная кнопка имеет градиентный фон для современного вида
2. **Тени**: Цветные тени создают эффект "парения" и глубины
3. **Hover эффекты**: 
   - Увеличение тени при наведении
   - Легкое увеличение масштаба (1.02x)
   - Изменение цвета текста для неактивных кнопок
4. **Плавные переходы**: Все изменения анимированы (300ms)

## Адаптивность

### Мобильные устройства (< 640px)
- Высота кнопок: 36px (`h-9`)
- Размер текста: 12px (`text-xs`)
- Все эффекты сохраняются

### Десктоп (≥ 640px)
- Высота кнопок: 40px (`h-10`)
- Размер текста: 14px (`text-sm`)
- Все эффекты сохраняются

## Преимущества нового дизайна

1. ✅ **Современный вид**: Градиенты и тени создают премиальный вид
2. ✅ **Четкая индикация**: Активная кнопка явно выделяется
3. ✅ **Приятная анимация**: Плавные переходы улучшают UX
4. ✅ **Hover эффекты**: Интерактивность на всех кнопках
5. ✅ **Адаптивность**: Отлично выглядит на всех устройствах
6. ✅ **Единый стиль**: Кнопки объединены в один контейнер

## Сравнение

### Было:
```tsx
<div className="flex gap-2">
  <Button variant="default/outline" ...>
    Все товары
  </Button>
  // ...
</div>
```

### Стало:
```tsx
<div className="flex gap-2 p-1.5 bg-muted/50 rounded-2xl border border-border/40 shadow-sm">
  <Button 
    variant="ghost"
    className="... bg-gradient-to-r from-primary to-primary/90 shadow-md shadow-primary/30 ..."
  >
    Все товары
  </Button>
  // ...
</div>
```

## Статус
✅ **РЕАЛИЗОВАНО** - Кнопки обновлены в современном стиле
✅ **СОБРАНО** - Проект успешно собран без ошибок
✅ **АДАПТИВНО** - Работает на мобильных и десктопных устройствах

## Скриншот стилей

### Активная кнопка:
- Градиентный фон (primary → primary/90)
- Цветная тень с эффектом свечения
- Белый текст
- Hover: увеличение тени и масштаба

### Неактивная кнопка:
- Прозрачный фон
- Приглушенный текст
- Hover: легкий фон и яркий текст

### Контейнер:
- Светлый фон с рамкой
- Большое скругление (rounded-2xl)
- Легкая тень
- Внутренние отступы для создания "рамки"
