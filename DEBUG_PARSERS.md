# 🔍 Отладка парсеров

## Проблемы

1. **Wildberries**: Возвращает "Что-то не так..." вместо названия
2. **Ozon**: 400 BAD REQUEST - не может извлечь данные
3. **Яндекс Маркет**: Название есть, но цена 0, изображений нет

## Что было улучшено

### 1. Улучшен поиск JS объектов
- Проверка разных вариантов структуры (`data.product`, `data.data.product`, `data.state.product`)
- Автоматический поиск любых объектов в `window` с ключевыми словами
- Проверка, является ли объект уже product (по наличию полей `name`, `price`)

### 2. Улучшен fallback на DOM
- Более агрессивный поиск селекторов
- Проверка валидности данных (не "Что-то не так...")
- Множественные попытки с разными селекторами

### 3. Улучшена обработка URL
- Декодирование URL в Flask API
- Очистка параметров перед парсингом

## Как отладить

### Вариант 1: Включить headful режим

```bash
export PLAYWRIGHT_HEADLESS=false
python api_server.py
```

Откроется браузер, можно увидеть что происходит.

### Вариант 2: Проверить логи

```bash
tail -f /tmp/api_server.log
```

Смотрите на сообщения:
- `⚠️ Wildberries: Ошибка извлечения...` - проблемы с JS объектами
- `✅ Wildberries: Название извлечено из DOM` - успешное извлечение
- `📦 Wildberries: Результат...` - финальные данные

### Вариант 3: Тестировать напрямую

```python
from parsers import get_parser

url = "https://www.wildberries.ru/catalog/315215210/detail.aspx"
parser = get_parser(url)
result = parser.parse()
print(result)
```

## Следующие шаги

Если парсеры все еще не работают:

1. **Проверить актуальную структуру страниц** через DevTools
2. **Использовать headful режим** для визуальной отладки
3. **Добавить скриншоты** при ошибках для анализа
4. **Рассмотреть использование упрощенных парсеров** (`USE_SIMPLE_PARSERS = True`)
