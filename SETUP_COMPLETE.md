# ✅ Настройка завершена автоматически

## Что было сделано:

1. ✅ **Добавлен Express прокси** в `server.js`
   - Теперь Express сервер проксирует запросы к Python API
   - Решает проблему CORS автоматически
   - Работает на том же домене, что и фронтенд

2. ✅ **Обновлен фронтенд** (`marketplaceParsers.ts`)
   - Автоматически определяет окружение (dev/prod)
   - В продакшене использует Express прокси
   - В разработке использует прямой доступ к Python API

3. ✅ **Проект пересобран**
   - Все изменения включены в `dist/`

## Как запустить:

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
./start-all.sh
```

Этот скрипт:
- Запустит Python API сервер на порту 5001
- Запустит Express сервер на порту 3000
- Покажет логи и инструкции

### Вариант 2: Ручной запуск

**Терминал 1 - Python API:**
```bash
source venv/bin/activate
python api_server.py
```

**Терминал 2 - Express сервер:**
```bash
npm start
```

## Для продакшена:

1. **Убедитесь, что Python API сервер запущен** на сервере:
   ```bash
   source venv/bin/activate
   python api_server.py
   ```

2. **Установите переменную окружения** (опционально):
   ```bash
   export PYTHON_API_URL=http://localhost:5001
   ```

3. **Запустите Express сервер:**
   ```bash
   npm start
   ```

4. **Express сервер автоматически проксирует** запросы к Python API

## Проверка работы:

1. Откройте админ-панель: `http://ваш-домен/admin`
2. Попробуйте импортировать товар
3. В консоли браузера (F12) должны быть логи:
   - `📤 Sending request to: http://ваш-домен/api/parse?url=...`
   - Не должно быть ошибок CORS
   - Данные должны приходить от Python API через Express прокси

## Логи:

- **Python API:** `tail -f /tmp/api_server.log`
- **Express:** вывод в консоль

## Преимущества этого решения:

✅ Нет необходимости в ngrok  
✅ Нет проблем с CORS  
✅ Работает на том же домене  
✅ Автоматическое определение окружения  
✅ Простая настройка

## Готово к использованию! 🎉
