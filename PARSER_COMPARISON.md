# Сравнение парсеров: Полные vs Упрощенные

## 📊 Два подхода

### 1. Полные парсеры (текущие)
**Файлы:** `wildberries.py`, `ozon.py`, `yandex_market.py`

**Особенности:**
- ✅ Retry механизм (3 попытки)
- ✅ Множественные способы извлечения данных
- ✅ Fallback на DOM при неудаче
- ✅ Расширенное логирование
- ✅ Валидация данных
- ⚠️ Более сложный код

### 2. Упрощенные парсеры (новые)
**Файлы:** `wildberries_simple.py`, `ozon_simple.py`, `yandex_market_simple.py`

**Особенности:**
- ✅ Простой и понятный код
- ✅ Прямое извлечение из JS объектов
- ✅ Fallback на DOM для критичных полей
- ✅ Базовая обработка ошибок
- ⚠️ Нет retry механизма
- ⚠️ Меньше способов извлечения

## 🔄 Как переключиться

В файле `parsers/__init__.py` измените:

```python
USE_SIMPLE_PARSERS = True  # Использовать упрощенные версии
```

Или через переменную окружения:
```bash
export USE_SIMPLE_PARSERS=true
```

## 📝 Рекомендации

### Используйте полные парсеры если:
- Нужна максимальная надежность
- Часто встречаются проблемы с извлечением данных
- Важна стабильность

### Используйте упрощенные парсеры если:
- Нужен простой и понятный код
- Хотите легко модифицировать логику
- Данные извлекаются стабильно

## 🎯 Что улучшено в упрощенных версиях

1. **Более прямолинейная логика** - легче понять и модифицировать
2. **Меньше кода** - проще поддерживать
3. **Сохранена надежность** - есть fallback на DOM
4. **Обработка ошибок** - базовые проверки сохранены

## ⚠️ Важные отличия

### Наценка
- **Полные парсеры:** Наценка применяется на фронтенде (как в ТЗ)
- **Упрощенные парсеры:** Наценка также применяется на фронтенде (соответствует ТЗ)

### Retry
- **Полные:** 3 попытки с перезагрузкой страницы
- **Упрощенные:** Одна попытка + fallback на DOM

### Извлечение данных
- **Полные:** Множественные способы (JS объекты, JSON-LD, скрипты, DOM)
- **Упрощенные:** JS объекты + DOM fallback

## 🚀 Тестирование

Попробуйте оба подхода и выберите тот, который лучше работает для ваших товаров:

```bash
# Тест полных парсеров (по умолчанию)
curl "http://localhost:5001/api/parse?url=..."

# Переключитесь на упрощенные в __init__.py
# USE_SIMPLE_PARSERS = True
# И протестируйте снова
```
