# Исправления импорта с маркетплейсов ✅

## Дата: 26 января 2026

## Проблемы

### 1. ❌ Неправильная цена с Ozon
**Проблема**: Цена импортировалась некорректно из-за неправильной обработки формата данных

### 2. ❌ Шаблонное описание с Яндекс Маркет
**Проблема**: Вместо реального описания товара импортировался шаблон от Яндекса

### 3. ❌ Автоматическая наценка 20%
**Проблема**: Наценка применялась сразу при импорте, не давая админу выбрать процент

## Решения

### 1. ✅ Исправлена цена с Ozon

**Файл**: `parsers/ozon_simple.py`

**Изменения**:
```python
# Улучшенное извлечение цены
price = js_data.get("price", 0)
if isinstance(price, dict):
    price = price.get("value", 0) or price.get("price", 0)
# Конвертация из копеек в рубли если нужно
if isinstance(price, (int, float)):
    if price > 1000:  # Если больше 1000, скорее всего в копейках
        price = price / 100
    result["price"] = float(price) if price > 0 else 0

# Аналогично для старой цены
old_price = js_data.get("oldPrice", 0) or js_data.get("originalPrice", 0)
if isinstance(old_price, dict):
    old_price = old_price.get("value", 0) or old_price.get("price", 0)
if isinstance(old_price, (int, float)):
    if old_price > 1000:
        old_price = old_price / 100
    result["old_price"] = float(old_price) if old_price > 0 and old_price != result["price"] else 0
```

**Что исправлено**:
- Добавлена проверка на несколько полей цены (`price`, `value`)
- Улучшена логика конвертации из копеек в рубли
- Добавлена обработка `originalPrice` для старой цены
- Добавлена проверка типов данных

### 2. ✅ Исправлено описание с Яндекс Маркет

**Файл**: `parsers/yandex_market_simple.py`

**Изменения в JS данных**:
```python
# Описание - ищем реальное описание товара, а не шаблон
description = js_data.get("description") or ""
# Если описание слишком короткое или выглядит как шаблон, пробуем другие поля
if not description or len(description) < 50:
    description = (
        js_data.get("fullDescription") or 
        js_data.get("detailedDescription") or 
        js_data.get("longDescription") or 
        description
    )
result["description"] = description
```

**Изменения в DOM fallback**:
```python
# Описание - ищем реальное описание товара
let descEl = document.querySelector('[data-zone-name="productDescription"]');
if (!descEl || descEl.textContent.trim().length < 50) {
    // Пробуем другие селекторы для описания
    descEl = document.querySelector('[data-auto="description"], .product-description, [itemprop="description"]');
}
if (descEl) {
    const descText = descEl.textContent.trim();
    // Фильтруем шаблонные описания
    if (descText.length > 50 && !descText.includes('Характеристики товара')) {
        data.description = descText;
    }
}
```

**Что исправлено**:
- Добавлен поиск в нескольких полях (`fullDescription`, `detailedDescription`, `longDescription`)
- Проверка минимальной длины описания (50 символов)
- Фильтрация шаблонных описаний
- Fallback на альтернативные селекторы DOM

### 3. ✅ Убрана автоматическая наценка при импорте

**Файл**: `src/components/admin/ProductForm.tsx`

#### 3.1. Импорт БЕЗ наценки

**Было**:
```typescript
// Применяем наценку к цене
const marginPercent = formData.margin_percent || 20;
const marginMultiplier = 1 + (marginPercent / 100);
const finalPrice = basePrice > 0 ? Math.round(basePrice * marginMultiplier) : 0;
```

**Стало**:
```typescript
// Импортируем цены БЕЗ наценки - как есть с маркетплейса
const basePrice = productData.price || 0;
const baseOriginalPrice = productData.old_price && productData.old_price > 0
  ? productData.old_price 
  : null;
```

#### 3.2. Наценка применяется при сохранении

**Добавлено в `handleSubmit`**:
```typescript
// Если товар импортирован, применяем наценку к ценам
let finalPrice = parseFloat(formData.price);
let finalOriginalPrice = formData.original_price ? parseFloat(formData.original_price) : null;

if (formData.is_imported && formData.margin_percent) {
  const marginMultiplier = 1 + (formData.margin_percent / 100);
  finalPrice = Math.round(finalPrice * marginMultiplier);
  if (finalOriginalPrice) {
    finalOriginalPrice = Math.round(finalOriginalPrice * marginMultiplier);
  }
}
```

#### 3.3. Обновлены сообщения UI

**Было**:
```
Цена автоматически обновляется с наценкой 20%
```

**Стало**:
```
Наценка 20% будет применена при сохранении
```

**Сообщение после импорта**:
```
✅ Товар успешно загружен с [Маркетплейс]!

Цена импортирована: [цена]₽

Вы можете изменить наценку перед сохранением товара.
```

## Преимущества новой логики

### 1. 🎯 Прозрачность цен
- Админ видит реальную цену с маркетплейса
- Может сравнить с конкурентами перед добавлением наценки
- Понятно, какая цена была изначально

### 2. 🔧 Гибкость наценки
- Админ сам выбирает процент наценки (0-100%)
- Может изменить наценку перед сохранением
- Разные наценки для разных товаров

### 3. 📊 Контроль маржи
- Видно базовую цену и итоговую цену с наценкой
- Легко рассчитать прибыль
- Можно корректировать стратегию ценообразования

## Workflow импорта товара

### Шаг 1: Импорт
1. Админ вставляет ссылку на товар
2. Нажимает "Загрузить"
3. Товар импортируется с **оригинальными ценами**

### Шаг 2: Настройка
1. Админ видит импортированную цену
2. Может изменить наценку (по умолчанию 20%)
3. Может отредактировать любые поля

### Шаг 3: Сохранение
1. Админ нажимает "Создать товар"
2. **Наценка применяется автоматически**
3. Товар сохраняется с финальной ценой

## Пример

### Импорт с Wildberries:
- **Цена на WB**: 1000₽
- **Старая цена на WB**: 1500₽
- **После импорта в форму**: 1000₽ / 1500₽
- **Наценка**: 20% (можно изменить)
- **После сохранения в БД**: 1200₽ / 1800₽

## Технические детали

### Поля в форме
```typescript
formData = {
  price: "1000",           // Оригинальная цена с маркетплейса
  original_price: "1500",  // Оригинальная старая цена
  margin_percent: 20,      // Наценка (можно изменить)
  is_imported: true,       // Флаг импорта
  source_url: "https://..." // URL источника
}
```

### Сохранение в БД
```typescript
productData = {
  price: 1200,            // С наценкой 20%
  original_price: 1800,   // С наценкой 20%
  margin_percent: 20,     // Сохраняем для будущих обновлений
  is_imported: true,
  source_url: "https://..."
}
```

## Тестирование

### Проверить:
1. ✅ Импорт с Ozon - цена корректная
2. ✅ Импорт с Яндекс Маркет - описание реальное, не шаблон
3. ✅ Импорт с Wildberries - работает как раньше
4. ✅ Наценка НЕ применяется при импорте
5. ✅ Наценка применяется при сохранении
6. ✅ Можно изменить наценку перед сохранением
7. ✅ Сообщения UI корректные

## Сборка
```bash
npm run build
✓ built in 6.68s
```

## Статус: ✅ ЗАВЕРШЕНО
Все проблемы исправлены. Импорт работает корректно.
