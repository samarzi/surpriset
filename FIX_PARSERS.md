# 🔧 Исправление парсеров - План действий

## Текущая ситуация

Парсеры не работают, потому что маркетплейсы изменили структуру страниц:
- **Wildberries**: Возвращает "Что-то не так..." вместо названия
- **Ozon**: 400 BAD REQUEST - не может извлечь данные
- **Яндекс Маркет**: Название есть, но цена 0, изображений нет

## Что было сделано

### ✅ Улучшения в парсерах

1. **Расширенный поиск JS объектов:**
   - Проверка разных вариантов структуры (`data.product`, `data.data.product`, `data.state.product`)
   - Автоматический поиск любых объектов в `window` с ключевыми словами
   - Проверка, является ли объект уже product

2. **Улучшен fallback на DOM:**
   - Более агрессивный поиск селекторов
   - Проверка валидности данных
   - Множественные попытки с разными селекторами

3. **Улучшена обработка URL:**
   - Декодирование URL в Flask API
   - Очистка параметров перед парсингом

## 🔍 Как отладить

### Вариант 1: Использовать тестовый скрипт

```bash
# Запустите скрипт диагностики
python test_parser_debug.py "https://www.wildberries.ru/catalog/315215210/detail.aspx"
```

Скрипт покажет:
- Какие window объекты доступны
- Какие селекторы находят название, цену, изображения
- Что именно на странице

### Вариант 2: Включить headful режим

```bash
export PLAYWRIGHT_HEADLESS=false
python api_server.py
```

Браузер откроется визуально, можно увидеть что происходит.

### Вариант 3: Проверить логи

```bash
tail -f /tmp/api_server.log
```

Смотрите на сообщения:
- `⚠️ Wildberries: Ошибка извлечения...` - проблемы с JS объектами
- `✅ Wildberries: Название извлечено из DOM` - успешное извлечение
- `📦 Wildberries: Результат...` - финальные данные

## 🎯 Рекомендации

### 1. Попробуйте упрощенные парсеры

В `parsers/__init__.py` измените:
```python
USE_SIMPLE_PARSERS = True
```

Или через переменную окружения:
```bash
export USE_SIMPLE_PARSERS=true
python api_server.py
```

### 2. Используйте headful режим для отладки

```bash
export PLAYWRIGHT_HEADLESS=false
python api_server.py
```

### 3. Запустите диагностику

```bash
python test_parser_debug.py "https://www.wildberries.ru/catalog/315215210/detail.aspx"
```

Это покажет что именно доступно на странице.

## 📝 Следующие шаги

1. **Запустите диагностику** для каждого маркетплейса
2. **Проверьте актуальную структуру** JS объектов
3. **Обновите селекторы** если нужно
4. **Протестируйте** с реальными товарами

## 🚀 Быстрое решение

Если нужно быстро заработало, попробуйте:

1. Переключиться на упрощенные парсеры
2. Включить headful режим
3. Увеличить задержки в `_wait_for_page_load`

```python
# В base.py увеличьте задержки
time.sleep(random.uniform(5, 8))  # Вместо 3-5
```
