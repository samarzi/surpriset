# Обновление свайпа с расширенным логированием

## Дата: 19 января 2026

## Проблема
Свайп назад все еще не работает с первой попытки, несмотря на восстановление кода из старой версии.

## Изменения для отладки

### 1. Уменьшены пороги срабатывания
```typescript
threshold = 50  // было 60px
velocity = 0.15 // было 0.2
```

Более низкие пороги делают свайп более чувствительным и легким в срабатывании.

### 2. Добавлено расширенное логирование

#### В handleTouchStart:
```typescript
console.log('👆 Touch start at x:', touch.clientX);
// ...
console.log('❌ Touch outside swipe zone (>100px)');
```
Теперь видно ВСЕ касания, даже те, что вне зоны свайпа.

#### В handleTouchEnd:
```typescript
console.log('🏁 Swipe end - deltaX:', deltaX, 'velocity:', swipeVelocity, 'threshold:', threshold, 'velocity threshold:', velocity);
console.log('🔍 Should navigate?', shouldNavigateBack, '| deltaX > threshold:', deltaX > threshold, '| velocity > min:', swipeVelocity > velocity);
console.log('🚀 Calling navigate(-1)');
console.log('⚠️ Already navigated, skipping');
```

Детальная информация о каждом этапе проверки.

## Что покажет логирование

### При касании экрана:
1. `👆 Touch start at x: [координата]` - показывает ВСЕ касания
2. `🟢 Swipe start detected` - если касание в зоне свайпа (< 100px)
3. `❌ Touch outside swipe zone` - если касание вне зоны

### При движении:
1. `➡️ Horizontal swipe RIGHT detected` - горизонтальный свайп вправо
2. `⬅️ Horizontal swipe LEFT` - свайп влево (игнорируется)
3. `⬆️⬇️ Vertical swipe` - вертикальный свайп (игнорируется)

### При завершении свайпа:
1. `🏁 Swipe end` - показывает deltaX, velocity и пороги
2. `🔍 Should navigate?` - показывает результат проверки условий
3. `✅ Navigating back!` - если условия выполнены
4. `🚀 Calling navigate(-1)` - момент вызова навигации
5. `⚠️ Already navigated` - если уже была навигация
6. `❌ Swipe not strong enough` - если не прошел по порогам

## Как использовать для отладки

1. Откройте DevTools в браузере (F12)
2. Перейдите на вкладку Console
3. Откройте карточку товара
4. Попробуйте свайп назад
5. Смотрите логи в консоли

### Что проверить:

**Если не видите `👆 Touch start`:**
- Touch события не регистрируются вообще
- Возможно, другой обработчик перехватывает события

**Если видите `👆` но не видите `🟢 Swipe start`:**
- Касание происходит вне зоны (> 100px от левого края)
- Попробуйте касаться ближе к левому краю

**Если видите `🟢` но не видите `➡️ Horizontal swipe`:**
- Движение определяется как вертикальное
- Попробуйте делать более горизонтальное движение

**Если видите `➡️` но не видите `✅ Navigating`:**
- Свайп слишком короткий (< 50px) или медленный (< 0.15)
- Смотрите значения deltaX и velocity в логе `🏁 Swipe end`

**Если видите `✅` но навигация не происходит:**
- Проблема в самой функции navigate()
- Возможно, блокируется роутером или другим компонентом

## Новые параметры

| Параметр | Старое значение | Новое значение | Причина |
|----------|----------------|----------------|---------|
| threshold | 60px | 50px | Легче срабатывание |
| velocity | 0.2 | 0.15 | Меньше требуемая скорость |

## Следующие шаги

1. **Протестировать на реальном устройстве** с открытой консолью
2. **Проанализировать логи** - на каком этапе происходит сбой
3. **Скорректировать параметры** на основе данных из логов
4. **Проверить конфликты** с другими обработчиками событий

## Статус
✅ **ОБНОВЛЕНО** - Добавлено расширенное логирование
✅ **СОБРАНО** - Проект успешно собран
⏳ **ТРЕБУЕТСЯ ТЕСТИРОВАНИЕ** - Нужно протестировать с открытой консолью

## Файлы
- `src/hooks/useSwipeBack.ts` - обновлен с логированием и новыми порогами
- `SWIPE_DEBUG_UPDATE.md` - этот документ
