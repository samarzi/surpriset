# 🚀 Руководство по развертыванию на Netlify

## 📋 Подготовка к развертыванию

### 1. Проверка локальной сборки

Перед развертыванием убедитесь, что проект собирается локально:

```bash
# Очистка предыдущих сборок
npm run clean

# Сборка проекта
npm run build

# Проверка сборки для Netlify
npm run build:netlify

# Локальный предварительный просмотр
npm run preview
```

### 2. Необходимые файлы

Убедитесь, что созданы следующие файлы:

- ✅ `public/_redirects` - для SPA роутинга
- ✅ `netlify.toml` - конфигурация Netlify
- ✅ `dist/` папка после сборки

## 🌐 Настройка Netlify

### Способ 1: Через Git (рекомендуется)

1. **Подключите репозиторий к Netlify:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Нажмите "New site from Git"
   - Выберите ваш Git провайдер (GitHub, GitLab, Bitbucket)
   - Выберите репозиторий

2. **Настройки сборки:**
   ```
   Build command: npm run build
   Publish directory: dist
   ```

3. **Переменные окружения:**
   - Перейдите в Site settings → Environment variables
   - Добавьте переменные:
     ```
     VITE_SUPABASE_URL=your_supabase_url
     VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
     ```

### Способ 2: Ручная загрузка

1. **Соберите проект локально:**
   ```bash
   npm run build:netlify
   ```

2. **Загрузите папку dist:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Перетащите папку `dist` в область "Deploy manually"

## 🔧 Настройки Netlify

### Build Settings

```
Base directory: (оставьте пустым)
Build command: npm run build
Publish directory: dist
Functions directory: netlify/functions (если используете)
```

### Environment Variables

Обязательные переменные:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

Опциональные переменные:
```
NODE_VERSION=18
NPM_VERSION=9
```

### Deploy Settings

- **Branch to deploy:** main (или ваша основная ветка)
- **Auto publishing:** Enabled
- **Deploy previews:** Enabled для pull requests

## 🐛 Решение проблем

### Проблема: Белый экран после развертывания

**Причины и решения:**

1. **Отсутствует файл `_redirects`:**
   ```bash
   # Создайте файл public/_redirects с содержимым:
   /*    /index.html   200
   ```

2. **Неправильные пути к ресурсам:**
   - Проверьте `vite.config.ts` - должен быть `base: './'`
   - Пересоберите проект: `npm run build`

3. **Ошибки JavaScript:**
   - Откройте DevTools в браузере
   - Проверьте консоль на ошибки
   - Проверьте Network tab на 404 ошибки

### Проблема: 404 ошибки при переходе по ссылкам

**Решение:**
- Убедитесь, что файл `public/_redirects` существует
- Проверьте содержимое файла `netlify.toml`
- Пересоберите и переразверните

### Проблема: Переменные окружения не работают

**Решение:**
1. Проверьте названия переменных (должны начинаться с `VITE_`)
2. Убедитесь, что переменные добавлены в Netlify dashboard
3. Пересоберите проект после добавления переменных

### Проблема: Сборка падает на Netlify

**Решение:**
1. Проверьте логи сборки в Netlify dashboard
2. Убедитесь, что локальная сборка работает: `npm run build`
3. Проверьте версию Node.js в `netlify.toml`

## 📊 Мониторинг и оптимизация

### Проверка производительности

1. **Lighthouse audit:**
   - Откройте сайт в Chrome
   - DevTools → Lighthouse → Generate report

2. **Bundle analyzer:**
   ```bash
   npm run build:analyze
   ```

### Оптимизация загрузки

1. **Кэширование:**
   - Настроено в `netlify.toml`
   - Статические ресурсы кэшируются на 1 год
   - HTML не кэшируется для обновлений

2. **Сжатие:**
   - Netlify автоматически сжимает ресурсы
   - Включено в конфигурации

## 🔄 Автоматическое развертывание

### Настройка CI/CD

1. **Автоматическое развертывание:**
   - Каждый push в main ветку → автоматическое развертывание
   - Pull requests → deploy previews

2. **Хуки развертывания:**
   - Настройте уведомления в Slack/Discord
   - Добавьте проверки качества кода

### Deploy Previews

- Каждый PR создает preview URL
- Тестируйте изменения перед мержем
- Автоматически удаляются после мержа

## 📱 Тестирование в Telegram

После развертывания протестируйте в Telegram WebApp:

1. Создайте бота в [@BotFather](https://t.me/botfather)
2. Настройте Web App URL на ваш Netlify домен
3. Протестируйте все функции

## 🎯 Чек-лист развертывания

- [ ] Локальная сборка работает (`npm run build`)
- [ ] Файл `public/_redirects` создан
- [ ] Файл `netlify.toml` настроен
- [ ] Переменные окружения добавлены в Netlify
- [ ] Репозиторий подключен к Netlify
- [ ] Первое развертывание успешно
- [ ] Сайт открывается без ошибок
- [ ] Роутинг работает (переходы между страницами)
- [ ] Telegram WebApp функции работают
- [ ] Производительность проверена

## 🆘 Получение помощи

Если проблемы остаются:

1. Проверьте логи сборки в Netlify dashboard
2. Откройте DevTools и проверьте консоль
3. Сравните с локальной версией
4. Проверьте [документацию Netlify](https://docs.netlify.com/)

---

**Успешного развертывания! 🚀**