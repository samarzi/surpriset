# Инструкция по тестированию исправления двойного скролла

## Что было сделано

Реализовано **многоуровневое решение** для блокировки скролла body/html в Telegram WebApp:

### 1. ✅ JavaScript с !important
- Стили применяются через `setProperty()` с флагом `'important'`
- Максимальный приоритет, который не может быть переопределен

### 2. ✅ MutationObserver
- Отслеживает изменения стилей на body и html
- Автоматически восстанавливает блокировку при попытке изменения

### 3. ✅ Блокировка событий
- Блокирует события `scroll` и `touchmove` на body/html
- Предотвращает прокрутку на уровне событий

### 4. ✅ Усиленный CSS
- Дополнительные правила с `!important`
- Явное указание всех позиционных свойств

## Как протестировать

### Шаг 1: Запустите приложение

```bash
npm run dev
```

### Шаг 2: Откройте в Telegram WebApp

Откройте ваше приложение в Telegram WebApp на мобильном устройстве или в эмуляторе.

### Шаг 3: Проверьте скролл

1. **Попробуйте скроллить содержимое** - должно работать нормально
2. **Попробуйте скроллить вверх от самого верха** - приложение НЕ должно сворачиваться
3. **Попробуйте скроллить вниз от самого низа** - не должно быть "резинового" эффекта

### Шаг 4: Откройте консоль разработчика

В консоли должны быть логи:

```
🔒 Body scroll BLOCKED via JavaScript with !important
👁️ MutationObserver установлен для защиты от изменений стилей
🚫 Scroll events blocked on body and html
```

### Шаг 5: Проверьте стили вручную

Выполните в консоли:

```javascript
// Проверка текущих стилей
console.log('Body position:', document.body.style.position) // должно быть 'fixed'
console.log('Body overflow:', document.body.style.overflow) // должно быть 'hidden'

// Попытка изменить (должна быть заблокирована)
document.body.style.position = 'relative'

// Проверка через 100ms (должно вернуться к 'fixed')
setTimeout(() => {
  console.log('Position after change:', document.body.style.position)
}, 100)
```

## Ожидаемый результат

✅ **Скролл работает только внутри #root**
✅ **Body и html не скроллятся**
✅ **Нет эффекта "двойного скролла"**
✅ **Приложение не сворачивается при скролле вверх**
✅ **MutationObserver защищает от изменений стилей**

## Если проблема сохраняется

### Вариант 1: Увеличить частоту проверки

В `src/utils/telegram.ts` измените:

```typescript
// Было: setInterval(() => { blockBodyScroll() }, 1000)
// Станет:
setInterval(() => { blockBodyScroll() }, 500) // каждые 500ms вместо 1000ms
```

### Вариант 2: Добавить touch-action

В `src/index.css` добавьте:

```css
html, body {
  touch-action: none !important;
}
```

### Вариант 3: Проверить версию Telegram

```javascript
console.log('Telegram version:', window.Telegram?.WebApp?.version)
```

Если версия старая (< 6.9), некоторые API могут не работать.

## Отладочные команды

```javascript
// Проверка классов
console.log('Body classes:', document.body.className)

// Проверка Telegram API
console.log('Telegram WebApp:', window.Telegram?.WebApp)

// Проверка disableVerticalSwipes
console.log('disableVerticalSwipes available:', 
  typeof window.Telegram?.WebApp?.disableVerticalSwipes === 'function')

// Проверка enableClosingConfirmation
console.log('enableClosingConfirmation available:', 
  typeof window.Telegram?.WebApp?.enableClosingConfirmation === 'function')
```

## Контакты для обратной связи

Если проблема сохраняется, предоставьте:
1. Версию Telegram
2. Модель устройства и ОС
3. Логи из консоли
4. Результаты отладочных команд
