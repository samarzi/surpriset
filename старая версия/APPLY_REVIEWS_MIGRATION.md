# Быстрая инструкция: Применение миграции отзывов

## ⚠️ ВАЖНО: Сначала примените базовые миграции!

Если вы видите ошибку `relation "products" does not exist`, значит базовые таблицы еще не созданы.

## 🔧 Шаг 1: Применить базовые миграции (если еще не применены)

### Через Supabase Dashboard:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Примените миграции **по порядку**:

#### 1.1 Базовая схема (001_initial_schema.sql)
- Нажмите **New Query**
- Скопируйте содержимое `migrations/001_initial_schema.sql`
- Вставьте и нажмите **Run**
- Дождитесь "Success"

#### 1.2 Система лайков (002_add_likes_system.sql)
- Нажмите **New Query**
- Скопируйте содержимое `migrations/002_add_likes_system.sql`
- Вставьте и нажмите **Run**

#### 1.3 Счетчик лайков (003_add_likes_count.sql)
- Нажмите **New Query**
- Скопируйте содержимое `migrations/003_add_likes_count.sql`
- Вставьте и нажмите **Run**

#### 1.4 Админы (004_admins.sql)
- Нажмите **New Query**
- Скопируйте содержимое `migrations/004_admins.sql`
- Вставьте и нажмите **Run**

## 🚀 Шаг 2: Применить миграцию отзывов (005_add_reviews_system.sql)

После того, как все базовые миграции применены:

1. В **SQL Editor** нажмите **New Query**
2. Скопируйте содержимое `migrations/005_add_reviews_system.sql`
3. Вставьте в редактор
4. Нажмите **Run** (или Ctrl/Cmd + Enter)
5. Дождитесь сообщения "Success"

## ✔️ Проверка

После применения всех миграций выполните:

```sql
-- Проверить все таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('products', 'product_likes', 'reviews')
ORDER BY table_name;

-- Должно вернуть 3 строки:
-- product_likes
-- products
-- reviews
```

Если все 3 таблицы есть - миграции применены успешно! ✨

## 🛠️ Альтернатива: Через командную строку

Если у вас установлен `psql` и есть `DATABASE_URL`:

```bash
# Применить все миграции по порядку
psql $DATABASE_URL -f migrations/001_initial_schema.sql
psql $DATABASE_URL -f migrations/002_add_likes_system.sql
psql $DATABASE_URL -f migrations/003_add_likes_count.sql
psql $DATABASE_URL -f migrations/004_admins.sql
psql $DATABASE_URL -f migrations/005_add_reviews_system.sql
```

## 📝 Что дальше?

После успешного применения всех миграций я создам:
1. Утилиты для работы с изображениями
2. Компоненты UI для отзывов
3. API интеграцию
4. Админ панель для модерации

## ❓ Проблемы?

Смотрите подробное руководство: `REVIEWS_MIGRATION_GUIDE.md`
