# 🔧 Исправление проблемы с отзывами

## Проблема
Вы не можете оставить отзыв, потому что настройки безопасности базы данных (RLS) блокируют операции.

## Быстрое решение

### Вариант 1: Автоматическое исправление (рекомендуется)

Выполните эту команду в терминале:

```bash
node scripts/apply-fix-reviews-rls.js
```

Если команда не работает, используйте Вариант 2.

### Вариант 2: Ручное исправление через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (левое меню)
4. Нажмите **New Query**
5. Скопируйте и вставьте весь код из файла `migrations/006_fix_reviews_rls_policies.sql`
6. Нажмите **Run** (или Ctrl+Enter)
7. Должно появиться сообщение об успешном выполнении

## Проверка

После исправления:

1. Откройте приложение в Telegram
2. Перейдите на страницу любого товара
3. Откройте вкладку **"Отзывы"**
4. Попробуйте оставить отзыв с оценкой

Если всё работает - отзыв будет отправлен на модерацию! ✅

## Дополнительно: Настройка хранилища фотографий

Если вы хотите, чтобы пользователи могли загружать фотографии к отзывам:

### Автоматически:
```bash
node scripts/setup-review-storage.js
```

### Вручную через Dashboard:

1. Откройте Supabase Dashboard → **Storage**
2. Нажмите **Create bucket**
3. Заполните:
   - **Name:** `review-photos`
   - **Public:** ✅ Включить
   - **File size limit:** `10 MB`
   - **Allowed MIME types:** `image/jpeg, image/png, image/webp`
4. Нажмите **Create bucket**

## Что было исправлено?

**Проблема:** База данных проверяла аутентификацию через Supabase Auth (`auth.uid()`), но приложение использует Telegram WebApp аутентификацию.

**Решение:** Убрали проверку `auth.uid()` из политик безопасности. Теперь валидация пользователя происходит в коде приложения.

## Нужна помощь?

Если проблема не решилась:

1. Откройте консоль браузера в Telegram:
   - Settings → Advanced → Debug Mode → Show Web Inspector
   
2. Попробуйте оставить отзыв и посмотрите на ошибки в консоли

3. Проверьте, что в консоли есть строка:
   ```
   Telegram user found: { id: ..., first_name: "..." }
   ```

Если этой строки нет - проблема в Telegram WebApp инициализации.

## Файлы

- `migrations/006_fix_reviews_rls_policies.sql` - SQL для исправления
- `scripts/apply-fix-reviews-rls.js` - автоматический скрипт
- `scripts/setup-review-storage.js` - настройка хранилища фотографий
- `scripts/check-reviews-setup.js` - проверка настройки (опционально)
