# Настройка для продакшена

## Проблема CORS решена

Все парсеры теперь используют Python API сервер через Playwright вместо старых прокси.

## Настройка для продакшена

### 1. Запустите Python API сервер

```bash
cd "/Users/samarzi/Desktop/IT/D E M O/surpriset"
source venv/bin/activate
python api_server.py
```

Сервер будет доступен на `http://localhost:5001`

### 2. Настройте ngrok для внешнего доступа

В другом терминале:

```bash
ngrok http 5001
```

Скопируйте HTTPS URL (например: `https://xxxx-xx-xx-xx-xx.ngrok-free.dev`)

### 3. Обновите переменную окружения

Создайте файл `.env` в корне проекта:

```env
VITE_API_BASE_URL=https://ваш-ngrok-url.ngrok-free.dev
```

Или обновите в `src/lib/marketplaceParsers.ts`:

```typescript
apiBaseUrl = 'https://ваш-ngrok-url.ngrok-free.dev';
```

### 4. Пересоберите проект

```bash
npm run build
```

### 5. Загрузите обновленные файлы

Загрузите содержимое папки `dist/` на ваш хостинг.

## Альтернатива: Постоянный ngrok URL

Если у вас есть платный аккаунт ngrok, можно зафиксировать URL:

```bash
ngrok http 5001 --domain=your-domain.ngrok-free.app
```

## Проверка работы

После настройки проверьте в браузере (консоль F12):

1. Должны быть логи: `📤 Sending request to: https://ваш-ngrok-url/api/parse?url=...`
2. Не должно быть ошибок CORS
3. Данные должны приходить от Python API

## Что изменилось

✅ Все парсеры (Wildberries, Ozon, Яндекс Маркет) используют Python API  
✅ Убраны старые прокси (corsproxy.io, api.allorigins.win, r.jina.ai)  
✅ Увеличен таймаут до 60 секунд для Playwright  
✅ Добавлена автоматическая детекция окружения (dev/prod)
