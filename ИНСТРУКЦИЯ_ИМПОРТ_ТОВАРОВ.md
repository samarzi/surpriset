# 🛒 Инструкция: Импорт товаров с маркетплейсов

## Быстрый старт

### 1. Применить миграцию БД

Откройте **Supabase SQL Editor** и выполните:

```sql
ALTER TABLE products
ADD COLUMN IF NOT EXISTS is_imported BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS source_url TEXT,
ADD COLUMN IF NOT EXISTS last_price_check_at TIMESTAMP WITH TIME ZONE;

CREATE INDEX IF NOT EXISTS idx_products_is_imported 
ON products(is_imported) WHERE is_imported = TRUE;

CREATE INDEX IF NOT EXISTS idx_products_price_check 
ON products(last_price_check_at) WHERE is_imported = TRUE;
```

### 2. Запустить проект

```bash
npm run dev
```

### 3. Импортировать товар

1. Админ-панель → Товары → Добавить товар
2. Вставить ссылку на товар (WB/Ozon/Яндекс Маркет)
3. Нажать "Загрузить"
4. Проверить данные
5. Сохранить

## Что работает

✅ **Wildberries** - полная поддержка  
✅ **Ozon** - полная поддержка  
✅ **Яндекс Маркет** - полная поддержка

## Автоматика

- **Наценка**: +20% к цене маркетплейса
- **Обновление цен**: каждые 24 часа автоматически
- **Статус наличия**: обновляется вместе с ценой

## Примеры ссылок

```
https://www.wildberries.ru/catalog/123456/detail.aspx
https://www.ozon.ru/product/название-123456/
https://market.yandex.ru/product/123456
```

## Важно

❌ Клиенты **НЕ видят** источник товара  
❌ Покупка на маркетплейсе - **вручную**  
✅ Цены обновляются **автоматически**

---

**Готово! Можно импортировать товары! 🎉**
